@using SimpleForum.Web.Resources
@model SimpleForum.Web.Models.Account.UserInfoModel

@if (Model == null)
{
	 @Html.ActionLink(ViewResources.UserInfo_LoginLink, "Login", "Account")
	 @:/
	 @Html.ActionLink(ViewResources.UserInfo_Registration, "Registration", "Account") 
}
else
{
	@Model.DisplayName @:/ @Html.ActionLink(ViewResources.UserInfo_LogoutLink, "Logout", "Account")
} 